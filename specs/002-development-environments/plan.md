# Implementation plan

Own devenv files, adapter and regressions, portable runner mode, CI, ignores/archive exclusions, and contributor/planning guidance. Preserve runtime QML, monitor allocation, settings, and release version. Reuse the native Node suite; retain Qt/plugin and live verification in the documented Linux/Omarchy gates.

Validate native devenv, actual Podman, archive boundaries, and current-head Docker/macOS CI before protected merge. Development tooling alone does not require a plugin release. Apple execution requires a suitable Mac and remains a separate verification limit.
