name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions: {}

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      checks: write
  release:
    needs: validate
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Verify tag and build archive
        run: |
          version=$(jq -r .version manifest.json)
          test "$GITHUB_REF_NAME" = "v$version"
          archive="omarchy-multi-monitor-workspaces-$version.tar.gz"
          git archive --prefix="omarchy-multi-monitor-workspaces-$version/" -o "$archive" HEAD
          sha256sum "$archive" > "$archive.sha256"
      - name: Attest release archive
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: "*.tar.gz"
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version=$(jq -r .version manifest.json)
          archive="omarchy-multi-monitor-workspaces-$version.tar.gz"
          gh release create "$GITHUB_REF_NAME" "$archive" "$archive.sha256" --generate-notes --verify-tag
